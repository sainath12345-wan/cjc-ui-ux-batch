// Problems:








// Sum all numbers without using reduce().

// Convert an array of names to uppercase (in-place).

// Count how many strings have length > 5.

// Create a frequency counter object for a list of numbers.

// Log every third element in an array.


// Problem 1:  Print each element in an array.

const arr = [10,20,30,40,50];

arr.forEach((element)=>{
    console.log(element)
})

