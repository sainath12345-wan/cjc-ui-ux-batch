/**
 * Sub String
 */

// when we create a card but too long name then we can show in that way.

const userName = "Sainath Dasa Wankhede";
const subString = userName.substring(0,10); // 0 is included and 10 is excluded.
console.log(subString + "....");