// Operations are used to perform calculations.
// Suppose operations is  a + b . here a,b are the operands (operation perfrom on what) . + is a operator (this is operator to perform perticaular operation)

a = 20;
b = 10;

// addition
sum = a + b;

// Substraction 
diff = a - b;

// Multiplication 
product = a * b;

// division 
div = a / b;

// Modulo (remainder operator)
remainder = a % b;

// Exponentiation (power operator)
power = a**b;

console.log(sum)
console.log(diff)
console.log(product)
console.log(div)
console.log(remainder)
console.log(power)